# Cargo
/target
Cargo.lock.bak

# Editor / OS noise
*.swp
*.swo
.DS_Store

# Per-iteration worktree dirs (not part of the durable log)
.autorize/*/iter-*/wt

# Central run log (project-root relative, append-only)
logs/
